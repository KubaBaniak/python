# You're on the right track!
from re import X

# _hjRecordingLastActivity
import string

encode=lambda a,b,c:(d:=0)or[(d:=pow(string.printable.index(e)*c+b+d,101,97)) for e in a]

text = 'RA1MIThYE'
len('text')
print([string.printable.index(x) for x in text])

